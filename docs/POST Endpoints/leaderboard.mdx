### Payload
Payload is given in the URL, after /leaderboard.
```
?ranking=
```
The only payload that has been seen being used is `score`
### Response
The leaderboard in an HTML format.

### Response Format
HTML
